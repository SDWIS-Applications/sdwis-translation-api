# SFTIES Translation API Configuration
# Copy this file to .env and update the values for your installation.
#
# Your two-letter state code
SDWIS_ST_CODE=MS

# --- SQL Server (most common) ---
MSSQL_SERVER=your-server-name
MSSQL_DATABASE=SDWIS_STATE
MSSQL_USER=your-username
MSSQL_PASSWORD=your-password
# MSSQL_PORT=1433
# MSSQL_TRUST_CERT=true

# --- Oracle (uncomment if using Oracle instead) ---
# ORACLE_USER=your-username
# ORACLE_PASSWORD=your-password
# ORACLE_CONNECT_STRING=(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=your-host)(PORT=1521))(CONNECT_DATA=(SERVICE_NAME=PROD)))

# --- Schema (optional) ---
# Only set this if your SDWIS tables are in a named schema.
# Most installations use the default schema and don't need this.
# SDWIS_SCHEMA=dbo

# --- Demo mode (no database needed) ---
# DEMO_MODE=true
